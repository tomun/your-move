<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_type_id %><br/>
    <%= f.text_field :game_type_id, value: @game.default_game_type.id %><br/>
  </div>
  <div class="field">
    <%= f.label :player_1_id %><br/>
    <%= f.text_field :player_1_id, value: @game.default_player_1.id %><br/>
  </div>
  <div class="field">
    <%= f.label :player_2_id %><br/>
    <%= f.text_field :player_2_id, value: @game.default_player_2.id %><br/>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
